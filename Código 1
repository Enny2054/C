#include <stdio.h>
#include <stdlib.h>

int main(){
  float notas[4];

  for(int i = 0; i < 4; i++){
    printf("Digite a nota %f: ", i+1);
    scanf("%f", &notas[i]);
  }
  printf("Jorge tirou as notas: %f, %f, %f, %f", notas[0], notas[1], notas[2], notas[3]);
  
  return 0;
}
